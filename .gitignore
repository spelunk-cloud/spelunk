/target
*.db
*.db-shm
*.db-wal
.env
.DS_Store
/models
# .spelunk/config.toml is committed so a bare clone resolves its cloud project;
# dbs are covered by *.db above, logs by .spelunk/.gitignore.
.spelunk/*.lock
.claude/settings.local.json
.claude/worktrees/
.env.local

# Python (bench/ tooling)
__pycache__/
*.pyc

logs/
*.log
